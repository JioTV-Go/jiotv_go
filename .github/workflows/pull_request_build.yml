name: Check build for Pull Request

on:
  pull_request:
    types: [opened, synchronize]

jobs:
    build:
        permissions:
            contents: read
        runs-on: ubuntu-latest
        strategy:
          matrix:
              goos: [linux, windows]
              goarch: [amd64, arm64]
              exclude:
                  - goos: windows
                    goarch: arm64
        steps:
        - uses: actions/checkout@v3
        - name: Set up Go
          uses: actions/setup-go@v3
          with:
            go-version: 1.21
        - name: Build binary for ${{ matrix.goos }}/${{ matrix.goarch }}
          run: |
            GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o ./bin/jiotv_go-${{ matrix.goos }}-${{ matrix.goarch }} -ldflags "-s -w" ./cmd/jiotv_go

        - name: Upload artifact
          uses: actions/upload-artifact@v3
          with:
            name: jiotv_go-${{ matrix.goos }}-${{ matrix.goarch }}
            path: ./bin/

    post:
      runs-on: ubuntu-latest
      permissions:
          contents: read
          pull-requests: write
      needs: build
      steps:
        - name: Comment on PR
          uses: thollander/actions-comment-pull-request@v2
          with:
            message: |
              Binaries for this PR ✅. Available at ${{ github.event.repository.html_url }}/actions/runs/${{ github.run_id }}
